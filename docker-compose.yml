services:
  life-dashboard:
    build: .
    container_name: life-dashboard
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - WEBHOOK_SECRET=v0PZK8sQYHsOuEejzNYCiWHaCVertBn3
    volumes:
      - dashboard-data:/app/data
    networks:
      - default
      - vps_n8n-network
    restart: unless-stopped

volumes:
  dashboard-data:

networks:
  vps_n8n-network:
    external: true
